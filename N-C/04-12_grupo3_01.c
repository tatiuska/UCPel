#include <stdio.h>
#include <math.h>

int main(){

int area, praga;
float desconto, valor_final;

printf("Informe a praga a ser combatida: \n");
scanf("%d", &);

printf("Informe a area a ser pulverizada: \n");
scanf("%d", &area);

switch(desconto){

case 1:
    desconto =
}

return 0;
}
