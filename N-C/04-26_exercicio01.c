#include <stdio.h>
#include <string.h>
//finalizar
int main(){

    //variaveis
    float nota_inicial, nota_final;
    int faltas;
    char nome, conceito;

    //iniciando variaveis
    nota_inicial = 0;
    nota_final = 0;
    faltas = 0;

    //Dados de Entrada
    printf("Digite seu nome: \n");
    scanf("%c", &nome);

    printf("Informe sua nota: \n");
    scanf("%d", &nota_inicial);

    printf("Informe seu número de faltas: \n");
    scanf("%d", &faltas);

    //Processamento e dados de saída
    if(faltas > 20) {
        if(nota_inicial >=)
    } else {
        printf("Seu conceito é: %c\n", conceito);
    }

    return 0;

}
